extends Node

var image : Image
var texture : ImageTexture

var textureData : PoolByteArray


# Called when the node enters the scene tree for the first time.
func _ready():
	#textureData = PoolByteArray.new()
	
	#yield(get_tree(), "idle_frame")
	#$ViewportContainer.setSampleTexture($Intelligence2.texture.get_data())
	
	#$ViewportContainer.setBlendImage($GreyImage.texture.get_data())
	
	#$ViewportContainer.material.set_shader_param("maskTexture", $maskTexture.texture)
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass
